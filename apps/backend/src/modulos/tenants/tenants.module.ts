import { Module } from '@nestjs/common';

/**
 * Módulo de administración de tenants (cadenas de supermercados).
 * Sprint 4: alta de tenants desde el panel interno, activación/baja.
 */
@Module({})
export class TenantsModule {}
