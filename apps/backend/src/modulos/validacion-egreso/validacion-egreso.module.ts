import { Module } from '@nestjs/common';

/**
 * Módulo de validación de egreso.
 * Sprint 3: vista del operador con el carrito declarado del usuario
 * (debe aparecer en <3s tras el pago) y registro del resultado del
 * cotejo humano. El MVP no automatiza el cotejo: la validación humana
 * es la mitigación de fraude.
 */
@Module({})
export class ValidacionEgresoModule {}
