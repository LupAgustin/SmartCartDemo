{
  "name": "@smartcart/panel",
  "version": "0.1.0",
  "private": true,
  "description": "Panel web SmartCart — B2B (admin del súper) + panel interno",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -p tsconfig.json --noEmit && vite build",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"sin tests de panel en Sprint 0\" && exit 0",
    "preview": "vite preview"
  },
  "dependencies": {
    "@smartcart/compartido": "*",
    "react": "19.2.0",
    "react-dom": "19.2.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.4.0",
    "typescript": "^5.6.0",
    "vite": "^6.3.0"
  }
}
